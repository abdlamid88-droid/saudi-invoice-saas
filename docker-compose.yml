version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: zatca_postgres
    restart: always
    environment:
      POSTGRES_DB: zatca_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  api:
    build: .
    container_name: zatca_crypto_api
    restart: always
    command: uvicorn zatca_api:app --host 0.0.0.0 --port 8000
    ports:
      - "8009:8000"
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=zatca_db
      - DB_USER=postgres
      - DB_PASSWORD=postgres
    depends_on:
      - db

  web:
    build: .
    container_name: blind_invoice_web
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - db
      - api
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=zatca_db
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - API_URL=http://api:8000
    ports:
      - "0.0.0.0:8505:8501"

  caddy:
    image: caddy:alpine
    container_name: blind_invoice_caddy
    restart: always
    ports:
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - web
      - api

volumes:
  postgres_data:
  caddy_data:
  caddy_config:
